import { ViewStyle, ImageStyle } from "react-native"

export const asyncImageStyles = {
  WRAPPER: {
    justifyContent: 'center'
  } as ViewStyle,
  IMAGE: {
    position: 'absolute',
  } as ImageStyle,
  PLACEHOLDER: {
    position: 'absolute'
  } as ViewStyle
}
